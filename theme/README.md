Theme for stderr.cz
===================

Minimal, text-first theme. Hand-written CSS (no framework), system fonts,
single column, automatic dark mode via `prefers-color-scheme`. No JS.

Variables
---------
In pelicanconf.py you can use these non-standard variables:

* `SOCIAL`: A dictionary {'title': 'url'} to appear in the footer social links.
